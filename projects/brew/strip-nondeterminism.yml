id: brew:strip-nondeterminism
display-name: strip-nondeterminism
homepage: https://salsa.debian.org/reproducible-builds/strip-nondeterminism
repo: >
  https://salsa.debian.org/reproducible-builds/strip-nondeterminism/-/archive/1.15.0/strip-nondeterminism-1.15.0.tar.bz2
docs: https://salsa.debian.org/reproducible-builds/strip-nondeterminism
package-manager:
  apk: strip-nondeterminism
  brew: strip-nondeterminism
  debian: strip-nondeterminism
  nix: strip-nondeterminism
  pacman: strip-nondeterminism
  ubuntu: strip-nondeterminism
  zypper: strip-nondeterminism
package-manager-url: https://formulae.brew.sh/formula/strip-nondeterminism
version: 1.15.0
license: GPL-3.0-or-later
category: development
tags:
  - cli
description: Tool for stripping bits of non-deterministic information from files
source-archive: >
  https://salsa.debian.org/reproducible-builds/strip-nondeterminism/-/archive/1.15.0/strip-nondeterminism-1.15.0.tar.bz2
uses-from-macos:
  - perl
executables:
  - exposure: global executable
    kind: cli
    name: strip-nondeterminism
bottle:
  available: true
  platforms:
    - arm64_linux
    - arm64_sequoia
    - arm64_sonoma
    - arm64_tahoe
    - arm64_ventura
    - sonoma
    - ventura
    - x86_64_linux
  root-url: https://ghcr.io/v2/homebrew/core
install-behavior:
  post-install-defined: false
install-lines:
  apk: sudo apk add strip-nondeterminism
  av: sudo av install brew:strip-nondeterminism
  brew: brew install strip-nondeterminism
  debian: sudo apt install strip-nondeterminism
  nix: nix profile install nixpkgs#strip-nondeterminism
  pacman: sudo pacman -S strip-nondeterminism
  ubuntu: sudo apt install strip-nondeterminism
  zypper: sudo zypper install strip-nondeterminism
provenance:
  formula: strip-nondeterminism
  provider: brew
  source: https://formulae.brew.sh/api/formula.json
