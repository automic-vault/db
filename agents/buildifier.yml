id: brew:buildifier
display-name: buildifier
repo: null
docs:
  - https://github.com/bazelbuild/buildtools/blob/master/buildifier/README.md
  - https://github.com/bazelbuild/buildtools/tree/master/buildifier
tags:
  - bazel
  - build-files
  - cli
  - formatter
  - linting
provenance:
  category-sources:
    - >
      source_facts.description says buildifier formats Bazel BUILD files with a standard
      convention.
    - >
      source_facts.homepage and source_facts.repo point to the official
      bazelbuild/buildtools repository.
  display-name-sources:
    - current_curation.display-name is buildifier.
    - source_facts.name is buildifier.
  docs-sources:
    - >
      Official repository buildifier directory and README are the project documentation
      surface.
    - source_facts.homepage is the official bazelbuild/buildtools repository.
  repo-sources:
    - >
      source_facts.repo is already populated with
      https://github.com/bazelbuild/buildtools, so repo is returned as null per
      instruction.
  tags-sources:
    - current_curation.tags supplies cli.
    - source_facts.description identifies Bazel BUILD file formatting.
    - source_facts.executables lists buildifier.
category-confidence: high
category-path:
  - developer-tools
  - formatters
display-name-confidence: high
docs-confidence: high
repo-confidence: high
tags-confidence: high
