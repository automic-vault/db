id: brew:gmailctl
display-name: gmailctl
repo: null
docs:
  - https://github.com/mbrt/gmailctl#readme
config-file-location:
  unix:
    - $XDG_CONFIG_HOME/gmailctl/config.jsonnet
    - ~/.config/gmailctl/config.jsonnet
    - ~/.gmailctl/config.jsonnet
credentials-file-location:
  unix:
    - $XDG_CONFIG_HOME/gmailctl/credentials.json
    - $XDG_CONFIG_HOME/gmailctl/token.json
    - ~/.config/gmailctl/credentials.json
    - ~/.config/gmailctl/token.json
    - ~/.gmailctl/credentials.json
    - ~/.gmailctl/token.json
tags:
  - automation
  - cli
  - configuration
  - email
  - filters
  - gmail
  - jsonnet
geiger:
  category: appliance
  color: green
  confidence: low
  reason: no executable entrypoint in the package index
  reasons:
    - no executable entrypoint in the package index
  signals:
    - metadata:no-indexed-executables
provenance:
  category-sources:
    - >
      source_facts.description says gmailctl provides declarative configuration for
      Gmail filters; official GitHub README describes generating and maintaining Gmail
      filters declaratively.
  display-name-sources:
    - source_facts.name is gmailctl; official GitHub README and repository use gmailctl.
  docs-sources:
    - >
      Official mbrt/gmailctl repository README documents installation, usage,
      configuration, search operators, actions, labels, tests, and tips.
  repo-sources:
    - >
      source_facts.repo already has value https://github.com/mbrt/gmailctl, so repo is
      returned as null per instruction.
  tags-sources:
    - >
      source_facts.description identifies Gmail filter configuration; official README
      describes a Jsonnet configuration file for categorizing, labeling, archiving, and
      managing Gmail inboxes automatically.
category-confidence: high
category-path:
  - productivity
  - email
display-name-confidence: high
docs-confidence: high
repo-confidence: high
tags-confidence: high
