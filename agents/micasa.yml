id: brew:micasa
display-name: micasa
repo: null
docs:
  - https://micasa.dev/docs
config-file-location:
  linux:
    - $XDG_CONFIG_HOME/micasa/config.toml
    - ~/.config/micasa/config.toml
  macos:
    - ~/Library/Application Support/micasa/config.toml
  windows:
    - '%APPDATA%\micasa\config.toml'
credentials-file-location:
  linux:
    - $XDG_CONFIG_HOME/micasa/config.toml
    - ~/.config/micasa/config.toml
  macos:
    - ~/Library/Application Support/micasa/config.toml
  windows:
    - '%APPDATA%\micasa\config.toml'
history:
  sources:
    - https://formulae.brew.sh/formula/micasa
    - https://github.com/micasa-dev
    - https://micasa.dev/
    - https://ostechnix.com/micasa-terminal-home-maintenance-tracker/
  summary:
    - >
      micasa is a Go terminal application by Phillip Cloud for keeping household
      maintenance data local. Its public site presents the package as "home maintenance,
      from the terminal," with workflows for schedules, projects, quotes, vendors,
      service history, documents, and offline/local-LLM use. The project deliberately
      avoids a hosted service model: the repository and site describe a keyboard-driven,
      Vim-style modal TUI backed by a single SQLite file, with no cloud account or
      subscription.
  usage:
    - >
      In package-manager terms, micasa is a niche productivity CLI rather than a
      developer build tool. Homebrew packages it as `micasa`, while the upstream
      installation path also supports `go install` and binary releases for Linux, macOS,
      and Windows. Its closest relatives are less other package-manager formulas than
      local-first personal databases and TUI tools; the project itself names VisiData as
      an inspiration.
tags:
  - cli
  - home-maintenance
  - local-first
  - sqlite
  - terminal-ui
geiger:
  category: appliance
  color: green
  confidence: low
  reason: no executable entrypoint in the package index
  reasons:
    - no executable entrypoint in the package index
  signals:
    - metadata:no-indexed-executables
provenance:
  category-sources:
    - >
      source_facts.description says it tracks home projects, maintenance schedules,
      appliances, and quotes; official micasa docs describe home maintenance, projects,
      incidents, vendors, documents, and appliances.
  display-name-sources:
    - >
      source_facts.name is micasa; official homepage and docs use the lowercase product
      name micasa.
  docs-sources:
    - >
      Official micasa docs page provides getting started, user guide, reference, and
      development documentation.
  history-sources:
    - https://formulae.brew.sh/formula/micasa
    - https://github.com/micasa-dev
    - https://micasa.dev/
    - https://ostechnix.com/micasa-terminal-home-maintenance-tracker/
  repo-sources:
    - >
      source_facts.repo is already populated with https://github.com/micasa-dev/micasa;
      per instruction, repo was not re-determined.
  tags-sources:
    - >
      source_facts.description identifies it as a TUI; official docs describe a
      keyboard-driven terminal UI, local SQLite storage, maintenance tracking, projects,
      and appliances.
category-confidence: high
category-path:
  - productivity
  - home-management
display-name-confidence: high
docs-confidence: high
history-confidence: low
repo-confidence: high
tags-confidence: high
