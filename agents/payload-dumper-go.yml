id: brew:payload-dumper-go
display-name: payload-dumper-go
repo: null
docs:
  - https://github.com/ssut/payload-dumper-go#readme
config-file-location: null
credentials-file-location: null
history:
  - >
    payload-dumper-go is a Go implementation of the Android OTA payload-dumping niche.
    The GitHub repository was created on October 3, 2020, and its README frames it as a
    faster alternative for extracting images from Android `payload.bin` files,
    especially full OTA packages.
  - >
    The tool's practical lineage is the Android update-engine payload format: it reads
    the payload metadata, extracts partition images such as boot, vendor_boot, vbmeta,
    recovery, system, and vendor, and uses Google's `update_metadata.proto` as an
    upstream format reference. Its README emphasizes parallel decompression, checksum
    verification, direct support for OTA zip files containing `payload.bin`, and an `xz`
    dependency chosen for performance.
  - >
    Adoption is strongest in Android modding, aftermarket ROM, and repair workflows
    rather than in general package management. Community guides use it to pull recovery,
    dtbo, vbmeta, and firmware-related images from /e/OS or similar ROM zips when those
    images are not published separately. Homebrew, MacPorts, and Nix packaging make it
    easy to install on development machines used for fastboot and recovery work.
tags:
  - android
  - cli
  - go
  - ota
  - payload-bin
geiger:
  category: appliance
  color: green
  confidence: low
  reason: narrow executable package without higher-risk signals
  reasons:
    - narrow executable package without higher-risk signals
  signals:
    - metadata:no-higher-risk-signals
provenance:
  category-sources:
    - >
      source_facts.description says payload-dumper-go is an Android OTA payload dumper
      written in Go.
  display-name-sources:
    - source_facts.name and source_facts.homepage identify payload-dumper-go.
  docs-sources:
    - >
      source_facts.homepage and source_facts.repo point to the official GitHub
      repository; README is the official documentation surface.
  history-sources:
    - https://github.com/ssut/payload-dumper-go
    - https://api.github.com/repos/ssut/payload-dumper-go
    - >
      https://community.e.foundation/t/walkthrough-extract-erecovery-and-boot-images-using-payload-dumper-go/52736
    - https://formulae.brew.sh/formula/payload-dumper-go
  repo-sources:
    - >
      source_facts.repo already has value https://github.com/ssut/payload-dumper-go; per
      instructions repo is not re-determined.
  tags-sources:
    - >
      source_facts.description identifies Android OTA payload dumping and Go
      implementation; source_facts.executables lists the CLI.
category-confidence: high
category-path:
  - developer-tools
  - android
display-name-confidence: high
docs-confidence: high
history-confidence: medium
repo-confidence: high
tags-confidence: high
