id: brew:rabbitmq
display-name: RabbitMQ
repo: null
docs:
  - https://www.rabbitmq.com/docs
config-file-location:
  linux:
    - /etc/rabbitmq/rabbitmq.conf
    - /etc/rabbitmq/advanced.config
    - /etc/rabbitmq/rabbitmq-env.conf
  macos:
    - ${install_prefix}/etc/rabbitmq/rabbitmq.conf
    - ${install_prefix}/etc/rabbitmq/advanced.config
    - ${install_prefix}/etc/rabbitmq/rabbitmq-env.conf
  unix:
    - $RABBITMQ_HOME/etc/rabbitmq/rabbitmq.conf
    - $RABBITMQ_HOME/etc/rabbitmq/advanced.config
    - $RABBITMQ_HOME/etc/rabbitmq/rabbitmq-env.conf
  windows:
    - '%APPDATA%\RabbitMQ\rabbitmq.conf'
    - '%APPDATA%\RabbitMQ\advanced.config'
    - '%APPDATA%\RabbitMQ\rabbitmq-env-conf.bat'
credentials-file-location: null
tags:
  - amqp
  - cli
  - message-broker
  - messaging
  - server
  - streaming
geiger:
  category: infrastructure
  color: orange
  confidence: medium
  reason: formula declares a Homebrew service
  reasons:
    - formula declares a Homebrew service
  signals:
    - metadata:service
provenance:
  category-sources:
    - >
      source_facts.description says RabbitMQ is a messaging and streaming broker;
      official RabbitMQ docs describe broker documentation for developers and
      administrators.
  display-name-sources:
    - source_facts.name is rabbitmq; official website and docs use the display name RabbitMQ.
  docs-sources:
    - >
      Official RabbitMQ documentation page is versioned documentation for RabbitMQ 4.3
      and links to getting started, install, management, and monitoring docs.
  repo-sources:
    - >
      source_facts.repo already contains https://github.com/rabbitmq/rabbitmq-server, so
      repo is returned as null per instruction.
  tags-sources:
    - >
      source_facts.description mentions messaging and streaming broker;
      source_facts.executables lists rabbitmq-server and administration tools.
category-confidence: high
category-path:
  - networking
  - messaging
display-name-confidence: high
docs-confidence: high
repo-confidence: high
tags-confidence: high
