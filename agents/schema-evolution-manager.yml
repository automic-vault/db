id: brew:schema-evolution-manager
display-name: schema-evolution-manager
repo: null
docs:
  - https://github.com/mbryzek/schema-evolution-manager#readme
config-file-location: null
credentials-file-location: null
history:
  - >
    Schema Evolution Manager, usually invoked as sem, is a small Ruby-oriented
    PostgreSQL migration toolkit from the Gilt Groupe engineering style of treating
    database schema changes as release artifacts. Its README says Gilt began using it in
    early 2012 across dozens of independent PostgreSQL databases, then packaged the
    workflow as source-controlled SQL scripts distributed as gzipped tarballs named from
    git tags.
  - >
    The tool deliberately keeps the workflow narrow: initialize a schema repository, add
    SQL files with sem-add, build a release tarball with sem-dist, and apply unapplied
    scripts with sem-apply. It records applied filenames in its own
    schema_evolution_manager tables and supports baselining existing databases. Its
    notable design choice is the absence of rollback support; the project argues for
    peer-reviewed, small, separately deployed schema evolutions instead of pretending
    every production DDL change can be reversed safely.
tags:
  - cli
  - database
  - postgresql
  - schema-migrations
geiger:
  category: tool
  color: blue
  confidence: medium
  reason: broad file, network, media, or database tool signal
  reasons:
    - broad file, network, media, or database tool signal
  signals:
    - text:database,sql
provenance:
  category-sources:
    - source_facts.description says it manages PostgreSQL database schema migrations.
    - source_facts.homepage is the official schema-evolution-manager repository.
  display-name-sources:
    - Official repository name is schema-evolution-manager.
    - source_facts.name is schema-evolution-manager.
  docs-sources:
    - >
      Official GitHub README is the best official documentation surface identified from
      source_facts.homepage.
  history-sources:
    - https://github.com/mbryzek/schema-evolution-manager
    - https://formulae.brew.sh/formula/schema-evolution-manager
  repo-sources:
    - >
      source_facts.repo already has https://github.com/mbryzek/schema-evolution-manager;
      per instruction repo is returned as null.
  tags-sources:
    - source_facts.description identifies PostgreSQL schema migrations.
    - source_facts.executables lists sem command-line tools.
category-confidence: high
category-path:
  - data
  - database-migrations
display-name-confidence: high
docs-confidence: medium
history-confidence: high
repo-confidence: high
tags-confidence: high
