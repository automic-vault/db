id: brew:traefik
display-name: Traefik
repo: null
docs:
  - https://doc.traefik.io/traefik
config-file-location:
  unix:
    - /etc/traefik/traefik.yml
    - /etc/traefik/traefik.yaml
    - /etc/traefik/traefik.toml
    - $XDG_CONFIG_HOME/traefik.yml
    - $XDG_CONFIG_HOME/traefik.yaml
    - $XDG_CONFIG_HOME/traefik.toml
    - ~/.config/traefik.yml
    - ~/.config/traefik.yaml
    - ~/.config/traefik.toml
    - ./traefik.yml
    - ./traefik.yaml
    - ./traefik.toml
credentials-file-location: null
tags:
  - application-proxy
  - cli
  - docker
  - kubernetes
  - load-balancer
  - microservices
  - reverse-proxy
geiger:
  category: infrastructure
  color: orange
  confidence: medium
  reason: formula declares a Homebrew service
  reasons:
    - formula declares a Homebrew service
  signals:
    - metadata:service
provenance:
  category-sources:
    - >
      Official Traefik docs describe Traefik as an open-source Application Proxy that
      routes requests to services and auto-discovers configuration.
    - source_facts.description says Modern reverse proxy.
  display-name-sources:
    - Official docs and GitHub repository use Traefik.
    - source_facts.name is traefik.
  docs-sources:
    - Official GitHub README points to doc.traefik.io/traefik for complete documentation.
    - >
      Official docs site provides product documentation, getting started, setup,
      routing, security, and reference sections.
  repo-sources:
    - source_facts.repo is already populated; repo intentionally not re-determined.
  tags-sources:
    - >
      Official Traefik docs mention service discovery, routing, Kubernetes, Docker, and
      providers.
    - source_facts.description identifies reverse proxy purpose.
category-confidence: high
category-path:
  - networking
  - proxy
  - load-balancer
display-name-confidence: high
docs-confidence: high
repo-confidence: high
tags-confidence: high
