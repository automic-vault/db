id: brew:imagemagick
display-name: ImageMagick
repo: null
docs:
  - https://imagemagick.org/command-line-processing
  - https://imagemagick.org/command-line-tools
config-file-location:
  unix:
    - $MAGICK_CONFIGURE_PATH
    - $PREFIX/etc/ImageMagick-7
    - $PREFIX/share/ImageMagick-7
    - $XDG_CACHE_HOME/ImageMagick
    - ~/.config/ImageMagick
    - <client path>/etc/ImageMagick
  windows:
    - $MAGICK_CONFIGURE_PATH
    - <windows registry>
    - $PREFIX/config
    - $USERPROFILE/.config/ImageMagick
    - <client path>
credentials-file-location: null
tags:
  - cli
  - graphics
  - image-conversion
  - image-processing
  - library
geiger:
  category: tool
  color: blue
  confidence: high
  reason: 'doc example: broad image transform tool'
  reasons:
    - 'doc example: broad image transform tool'
  signals:
    - override:imagemagick
provenance:
  category-sources:
    - >
      source_facts.description says ImageMagick provides tools and libraries to
      manipulate images; official homepage describes a suite for editing and
      manipulating digital images.
  display-name-sources:
    - >
      Official homepage uses the ImageMagick product name; source_facts.name provides
      the package identifier imagemagick.
  docs-sources:
    - >
      Official ImageMagick site provides dedicated command-line tools and command-line
      processing documentation pages.
  repo-sources:
    - >
      source_facts.repo already has an official repository URL, so repo discovery was
      skipped per instructions.
  tags-sources:
    - >
      source_facts.executables lists ImageMagick CLI tools; official homepage and docs
      describe image manipulation, conversion, command-line tools, and APIs.
category-confidence: high
category-path:
  - media
  - image-processing
display-name-confidence: high
docs-confidence: high
repo-confidence: high
tags-confidence: high
