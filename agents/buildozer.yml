id: brew:buildozer
display-name: buildozer
repo: null
docs:
  - https://github.com/bazelbuild/buildtools/blob/master/buildozer/README.md
  - https://github.com/bazelbuild/buildtools/tree/master/buildozer
tags:
  - automation
  - bazel
  - build-files
  - cli
  - rewrite-tool
geiger:
  category: appliance
  color: green
  confidence: low
  reason: narrow executable package without higher-risk signals
  reasons:
    - narrow executable package without higher-risk signals
  signals:
    - metadata:no-higher-risk-signals
provenance:
  category-sources:
    - >
      source_facts.description says buildozer rewrites Bazel BUILD files using standard
      commands.
    - >
      source_facts.homepage and source_facts.repo point to the official
      bazelbuild/buildtools repository.
  display-name-sources:
    - current_curation.display-name is buildozer.
    - source_facts.name is buildozer.
  docs-sources:
    - >
      Official repository buildozer directory and README are the project documentation
      surface.
    - source_facts.homepage is the official bazelbuild/buildtools repository.
  repo-sources:
    - >
      source_facts.repo is already populated with
      https://github.com/bazelbuild/buildtools, so repo is returned as null per
      instruction.
  tags-sources:
    - current_curation.tags supplies cli.
    - source_facts.description identifies Bazel BUILD file rewriting.
    - source_facts.executables lists buildozer.
category-confidence: high
category-path:
  - developer-tools
  - build-tools
display-name-confidence: high
docs-confidence: high
repo-confidence: high
tags-confidence: high
