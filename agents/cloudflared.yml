id: brew:cloudflared
display-name: cloudflared
repo: null
docs:
  - https://developers.cloudflare.com/cloudflare-one/networks/connectors/cloudflare-tunnel
  - https://github.com/cloudflare/cloudflared#readme
config-file-location:
  unix:
    - ~/.cloudflared/config.yml
    - $XDG_CONFIG_HOME/cloudflared/config.yml
    - ~/.config/cloudflared/config.yml
credentials-file-location:
  unix:
    - ~/.cloudflared/cert.pem
    - ~/.cloudflared/*.json
    - $XDG_CONFIG_HOME/cloudflared/cert.pem
    - $XDG_CONFIG_HOME/cloudflared/*.json
    - ~/.config/cloudflared/cert.pem
    - ~/.config/cloudflared/*.json
tags:
  - cli
  - cloudflare
  - proxy
  - tunnel
  - zero-trust
geiger:
  category: infrastructure
  color: orange
  confidence: medium
  reason: formula declares a Homebrew service
  reasons:
    - formula declares a Homebrew service
    - infrastructure mutation or orchestration signal
  signals:
    - metadata:service
    - text:cloud
provenance:
  category-sources:
    - >
      source_facts.description says Cloudflare Tunnel client; official README describes
      tunneling daemon proxying traffic from Cloudflare to origins.
  display-name-sources:
    - source_facts.name and official GitHub repository name are cloudflared.
  docs-sources:
    - >
      source_facts.homepage is official Cloudflare Tunnel docs; official GitHub README
      says extensive docs are in Cloudflare Docs.
  repo-sources:
    - >
      source_facts.repo already supplies https://github.com/cloudflare/cloudflared; per
      instruction repo is returned as null.
  tags-sources:
    - >
      source_facts.description identifies Cloudflare Tunnel client; official README
      documents tunnel and access command usage.
category-confidence: high
category-path:
  - networking
  - tunneling
  - zero-trust
display-name-confidence: high
docs-confidence: high
repo-confidence: high
tags-confidence: high
