id: brew:rclone
display-name: rclone
repo: null
docs:
  - https://rclone.org/docs
  - https://rclone.org/commands/rclone_config_file
config-file-location:
  unix:
    - $XDG_CONFIG_HOME/rclone/rclone.conf
    - ~/.config/rclone/rclone.conf
  windows:
    - '%APPDATA%\rclone\rclone.conf'
credentials-file-location:
  unix:
    - $XDG_CONFIG_HOME/rclone/rclone.conf
    - ~/.config/rclone/rclone.conf
  windows:
    - '%APPDATA%\rclone\rclone.conf'
tags:
  - backup
  - cli
  - cloud-storage
  - file-transfer
  - sync
geiger:
  category: infrastructure
  color: orange
  confidence: medium
  reason: infrastructure mutation or orchestration signal
  reasons:
    - infrastructure mutation or orchestration signal
  signals:
    - text:cloud
provenance:
  category-sources:
    - >
      rclone official documentation describes rclone as a command-line program for
      managing files on cloud storage and documents sync/copy/move style operations.
  display-name-sources:
    - source_facts.name is rclone; rclone official documentation uses the display name rclone.
  docs-sources:
    - >
      The official rclone documentation is hosted at rclone.org/docs and includes the
      rclone config file command reference.
  repo-sources:
    - >
      source_facts.repo is already populated with https://github.com/rclone/rclone, so
      repo is returned as null per instruction.
  tags-sources:
    - >
      rclone official docs describe cloud storage remotes, the configuration file, and
      file management operations such as sync, copy, move, and backend configuration.
category-confidence: high
category-path:
  - system
  - file-transfer
display-name-confidence: high
docs-confidence: high
repo-confidence: high
tags-confidence: high
