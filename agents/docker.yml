id: brew:docker
display-name: Docker
repo: null
docs:
  - https://docs.docker.com/get-started/docker-overview
  - https://docs.docker.com/reference/cli/docker
config-file-location:
  unix:
    - ~/.docker/config.json
credentials-file-location:
  unix:
    - ~/.docker/config.json
tags:
  - cli
  - container-runtime
  - containers
  - developer-tools
  - docker
geiger:
  category: infrastructure
  color: orange
  confidence: high
  reason: 'doc example: container infrastructure'
  reasons:
    - 'doc example: container infrastructure'
  signals:
    - override:docker
provenance:
  category-sources:
    - >
      Docker official overview describes Docker as a platform for developing, shipping,
      and running applications with containers, and the CLI docs describe docker as the
      primary Docker client.
  display-name-sources:
    - source_facts.name is docker; Docker official documentation uses the product name Docker.
  docs-sources:
    - >
      Docker official documentation provides the docker CLI reference and Docker
      overview pages.
  repo-sources:
    - >
      source_facts.repo is already populated with https://github.com/docker/cli, so repo
      is returned as null per instruction.
  tags-sources:
    - >
      Docker official documentation describes Docker container workflows, the docker
      command-line client, images, registries, and container runtime usage.
category-confidence: high
category-path:
  - developer-tools
  - containers
display-name-confidence: high
docs-confidence: high
repo-confidence: high
tags-confidence: high
