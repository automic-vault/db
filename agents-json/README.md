# Agent JSON Stage

Codex enrichment writes one schema-validated JSON file per formula here.

This is the canonical agent artifact. `agents/*.yml` is derived from these JSON
files for review and for merging into `combined/`.
